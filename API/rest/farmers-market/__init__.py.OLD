#from flask import Flask, request
from flask_restful import Resource, Api
from flask import Flask, request

import sys
sys.path.append('class')
from REST import REST

app = Flask(__name__)

@app.route("/")
def index():
    rest = REST()

    return Response(rest.response), 200

if __name__ == "__main__":
    app.run(debug=True)
